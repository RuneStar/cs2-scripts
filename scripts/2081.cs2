script2081(int $int0, int $int1)
if (cc_find(257:0, 0) = true) {
	if (clientclock < $int0) {
		return;
	} else if (clientclock >= $int1) {
		cc_setontimer(null);
		return;
	} else {
		cc_setcolour(0xf0a808);
		cc_setfillcolour(0xf0a808);
		cc_settrans(interpolate(0, 255, $int0, $int1, clientclock));
		_1124(255);
	}
}
return;
