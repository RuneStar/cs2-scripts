script619(int $int0, int $int1, string $string0)
if (if_hassub(enum(component, component, ~toplevel_getcomponents, 161:13)) = false) {
	return;
} else {
	def_int $int2 = calc($int0 / 256);
	def_int $int3 = ~script633(testbit($int0, 1));
	def_int $int4 = calc(($int0 & 252) / 4);
	~poll_storequestion($int2, $string0);
	~script621($int2, $int3, $int4, $int1, $string0);
	return;
}
