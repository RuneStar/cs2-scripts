// 1420
[clientscript,br_tournament_submit](int $clock0, string $string0, component $component1, int $comsubid2)
if (clientclock < $clock0) {
	return;
}
resume_namedialog($string0);
if (cc_find($component1, $comsubid2) = ^true) {
	cc_setcolour(^white);
	cc_setontimer(null);
}
