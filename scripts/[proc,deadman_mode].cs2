// 2248
[proc,deadman_mode]()(int)
def_int $flags0 = worldflags;
if (testbit($flags0, 29) = ^true) {
	if (testbit($flags0, 26) = ^true) {
		return(3);
	}
	if (testbit($flags0, 30) = ^true) {
		return(2);
	}
	return(1);
}
return(0);
