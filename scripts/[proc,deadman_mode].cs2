[proc,deadman_mode]()(int)
def_int $int0 = worldflags;
if (testbit($int0, 25) = true) {
	return(3);
} else if (testbit($int0, 29) = true) {
	if (testbit($int0, 26) = true) {
		return(3);
	} else if (testbit($int0, 30) = true) {
		return(2);
	} else {
		return(1);
	}
} else {
	return(0);
}
