// 3556
[clientscript,poh_costumes_buttons_toggle_reset](component $component0, graphic $graphic1, int $clock2)
if (clientclock >= $clock2) {
	~poh_costumes_buttons_toggle_reset($component0, $graphic1);
}
