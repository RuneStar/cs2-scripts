// 3130
script3130(int $int0, int $int1, int $colour2, component $component3)
if (cc_find($component3, 0) = ^true) {
	if (clientclock < $int0) {
		return;
	}
	if (clientclock >= $int1) {
		cc_setontimer(null);
		return;
	}
	cc_setcolour($colour2);
	cc_setfillcolour($colour2);
	cc_settrans(interpolate(255, 0, $int0, $int1, clientclock));
	_1124(255);
}
