// 4574
[proc,settings_side_dropdown_update](component $component0, enum $enum1, int $int2)
if (cc_find($component0, 4) = ^true) {
	cc_settext(enum(int, string, $enum1, ~settings_get_dropdown($int2)));
}
