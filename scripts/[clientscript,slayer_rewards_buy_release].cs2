// 326
[clientscript,slayer_rewards_buy_release](component $component0, int $comsubid1, int $clock2)
if (clientclock < $clock2) {
	return;
}
if (cc_find($component0, $comsubid1) = ^true) {
	cc_settrans(0);
	cc_setontimer(null);
}
