// 221
script221(component $component0, int $int1, int $colour2, int $int3)
if (clientclock < $int3) {
	return;
}
if (cc_find($component0, $int1) = ^true) {
	cc_setcolour($colour2);
	cc_setontimer(null);
}
