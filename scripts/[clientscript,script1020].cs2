// 1020
[clientscript,script1020](component $component0, int $comsubid1, int $colour2, int $clock3)
if (clientclock < $clock3) {
	return;
}
if (cc_find($component0, $comsubid1) = ^true) {
	cc_setcolour($colour2);
	cc_setontimer(null);
}
